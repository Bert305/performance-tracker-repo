doctype html
html
    head
        title Login
    body
        h1 Login
        form(action="/login" method="post", onsubmit="window.location='/create-tasks-pug'; return false;")
            div
                label(for="username") Username:
                input(type="text" name="username" id="username" required)
            div
                label(for="password") Password:
                input(type="password" name="password" id="password" required)
            div
                button(type="submit") Login



                //- Want to attach the bootrstap cdn here that Maria designed. 
                
